<%= form_with(model: current_user.active_relationships.build, local: true) do %>
  <div><%= hidden_field_tag :followed_id, user.id %></div>
  <%= f.submit "フォローする", class: "btn btn-success btn-block" %>
<% end %>